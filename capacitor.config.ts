import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'Brahma Electric',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
